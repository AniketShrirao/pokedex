---

pagination:
    data:pokemons
    size:1
    alias:pokemon
permalink: "/different/{{ pokemon.name | slug }}/index.html"
---

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="/assets/css/style.css">
    <title>{{ settings.sitename }}</title>
</head>
<body>
    <section class= "pokemons-dex">
        <div class="wrapper">
            <h1 class ="heading">
            <a href="/" title="{{ settings.sitename }}">
                <img src="{{ settings.heading }}" alt="{{ settings.sitename }}">
            </a>
            </h1>
            <ul id="pokedex">
                <li class="card">
                <figure>
                <img class="card-image" src="{{ pokemon.image }}" alt="{{ pokemon.name }}">
                </figure>
                <h2 class="card-title" data-id="{{ pokemon.id }}">
                    <a href="/different/{{ pokemon.name }}" title="{{ pokemon.name }}">{{ pokemon.name }}</a>
                </h2>
                <p class="card-subtitle">Type: {{ pokemon.type }}</p>
                </li>
            </ul>
        </div>
    </section>
</body>
</html>